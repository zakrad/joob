name = "joob-drive"
main = "src/index.js"
compatibility_date = "2024-09-01"

# No KV/D1/R2 bindings — the Worker is stateless.
# Optional secret: WORKER_AUTH_TOKEN (set via `wrangler secret put WORKER_AUTH_TOKEN`).
